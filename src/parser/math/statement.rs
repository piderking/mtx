use nom::{IResult, character::complete::alpha1};

use crate::ast::base::{Ident};




#[cfg(test)]
mod tests {
    // Note this useful idiom: importing names from outer (for mod tests) scope.
    use super::*;

    #[test]
    fn parse_variable_name() {
    }
   
    
}