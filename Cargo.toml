[package]
name = "mtx"
version = "0.1.0"
edition = "2024"

[dependencies]
nom = "8.0.0"
quote = "1.0.41"
syn = {"version"= "2.0.108", "features"= ["full"]}
tokio-util = {"version" = "*", "features" = ["io"]}
